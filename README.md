# wikipediaSpeedrunning
Finds the shortest path between two articles on wikipedia

To run the code, input the start and input the end.  Then wait longer than the length of the universe for a response
